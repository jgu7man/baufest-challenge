import { ListClassPipe } from './class-name.pipe';

describe('ListClassPipe', () => {
  it('create an instance', () => {
    const pipe = new ListClassPipe();
    expect(pipe).toBeTruthy();
  });
});
